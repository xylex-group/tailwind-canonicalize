{
  "name": "tailwind-canonicalize-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter=./packages/* --filter=!tailwind-canonicalize-vscode run build && pnpm --filter tailwind-canonicalize-vscode run build",
    "dev": "pnpm -r --filter=./packages/* run dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc -b --pretty false",
    "lint": "ultracite check",
    "format": "ultracite fix",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "knip": "knip",
    "publint": "pnpm -r --filter=./packages/* exec publint",
    "benchmark": "tsx benchmarks/run.ts",
    "docs:dev": "pnpm --filter docs dev",
    "docs:build": "pnpm --filter docs build",
    "docs:deploy": "pnpm --filter docs deploy",
    "docs:deploy:worker": "pnpm --filter docs deploy:worker",
    "docs:deploy:dry-run": "pnpm --filter docs deploy:dry-run",
    "docs:cf-typegen": "pnpm --filter docs cf-typegen",
    "deploy:docs": "pnpm run docs:deploy",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "@changesets/cli": "^2.29.6",
    "@types/node": "^22.17.2",
    "@vitest/coverage-v8": "^3.2.4",
    "husky": "^9.1.7",
    "knip": "^5.63.0",
    "lint-staged": "^16.1.5",
    "publint": "^0.3.12",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2",
    "ultracite": "^5.3.9",
    "vitest": "^3.2.4"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,jsonc,css,md,mdx}": [
      "pnpm exec ultracite fix"
    ]
  }
}
