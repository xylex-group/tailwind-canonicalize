# @tailwind-canonicalize/parser

Internal workspace package (not published). AST-based class string extraction for `tailwind-canonicalize`.

Uses [oxc](https://oxc.rs/) for JS/TS/JSX/TSX and structured attribute extraction for HTML-like formats.
