# @tailwind-canonicalize/resolver

Internal workspace package (not published). Theme loading and safe equivalence for Tailwind utilities.

Core API: `findCanonicalEquivalent`, `canonicalizeClass`, `loadThemeFromCss`.
